name: Publish Conda package

on:
  release:
    types:
      - published

  repository_dispatch:
    types:
      - build

  pull_request:

jobs:
  publish:
    name: Build and publish conda packages
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: get release info
        id: release_info
        run: |
          echo "VERSION=9.9.9.9" >> $GITHUB_ENV
          echo "LABELS=rc" >> $GITHUB_ENV
          echo "UPLOAD=--no-anaconda-upload" >> $GITHUB_ENV

      - name: Cache conda
        uses: actions/cache@v2
        env:
          # Increase this value to reset cache if environment.yml has not changed
          CACHE_NUMBER: 0
        with:
          path: ~/conda_pkgs_dir
          key:
            ${{ runner.os }}-conda-${{ env.CACHE_NUMBER }}-${{ hashFiles('environment.yml') }}

      - name: Install miniconda
        uses: conda-incubator/setup-miniconda@v2
        with:
          miniconda-version: "latest"
          python-version: 3.8
          use-mamba: true
          mamba-version: "*"
          channels: conda-forge
          use-only-tar-bz2: true
          auto-activate-base: false
          environment-file: environment.yml
          activate-environment: pyincore
          conda-build-version: "*"

      - name: Print information about conda
        run: |
          conda env list
          conda info
          conda list
          conda config --show-sources
          conda config --show

      - name: Build conda package
        run: |
          conda build recipes
#          conda build --keep-going ${{ env.UPLOAD }} \
#                      --token ${{ secrets.CONDA_INCORE_TOKEN }} \
#                      --user IN-CORE \
#                      --no-force-upload \
#                      --label ${{ env.LABELS }} \
#                      recipes/meta.yaml

#      - name: Upload binaries to release
#        if: github.event_name == 'release'
#        uses: svenstaro/upload-release-action@v2
#        with:
#          repo_token: ${{ secrets.GITHUB_TOKEN }}
#          file: dist/pyincore-${{ env.VERSION }}*
#          file_glob: true
#          overwrite: true
