# List of pyincore analyses to either include or exclude for release
# If provided, 'excludes' takes preference over 'includeOnly'

version: '0.6.4'

includeOnly:
  - buildingdamage
  - bridgedamage

exclude:

# includeOnly feature is not implemented yet
